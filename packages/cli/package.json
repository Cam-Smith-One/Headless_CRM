{
  "name": "@headless-crm/cli",
  "version": "0.1.1",
  "description": "CLI entry point for Headless CRM — start the full stack with a single command",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Cam-Smith-One/Headless_CRM"
  },
  "private": true,
  "type": "module",
  "bin": {
    "headless-crm": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'CLI runs via tsx — no build needed'",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@headless-crm/api": "*",
    "@hono/node-server": "^2.0.6"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
